---
name: discover-architecture
description: Search inference-time agent scaffolds on a held-out benchmark
---

Read `discovering-agent-architectures`. Held-out only. Report calls and length-adjusted scores. Not clinical advice.

```bash
python -m swarm discover "$QUERY"
```
