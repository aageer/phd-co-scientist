---
name: sota-survey
description: Survey GDM, Stanford, Sakana, FutureHouse, and CS-agent SOTA
---

Read `surveying-sota` and `running-grok-swarm`. Fan out one Grok worker per lab. Merge into a table with citations.

```bash
python -m swarm sota
python -m swarm swarm "co-scientist SOTA" -n 6
```
